---
apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.transactions.name }}"
  labels:
    chart: '{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}'
    app: "{{ .Values.transactions.name }}"
    version: "{{ .Values.transactions.version }}"
spec:
  ports:
    - name: "http-{{ .Values.transactions.name }}"
      port: {{ .Values.transactions.servicePort }}
      targetPort: "http-{{ .Values.transactions.name }}"
  selector:
    app: "{{ .Values.transactions.name }}"
